[toolchain]
# Bumped from 1.84.0 to 1.91.0 to satisfy revm 40+ (requires Rust 1.91+)
# and the broader alloy 2.0 / c-kzg 2.x dependency tree we're migrating
# toward. See the alloy + revm modernization plan in docs/.
channel = "1.91.0"